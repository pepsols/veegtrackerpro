interface:
  display_name: "Local LLM"
  short_description: "Choose and run local LLM stacks"
  default_prompt: "Use $local-llm to recommend a practical local model stack for my hardware and use case."

policy:
  allow_implicit_invocation: true
